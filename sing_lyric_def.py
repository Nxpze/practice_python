import time
from animate_text import animate_text


def sing_lyric(lyric, delay, speed):
    time.sleep(delay)
    animate_text(lyric, speed)



for i in range(1,41):
    print("one sample wilcoxon signed rank test ใช้สำหรับทดสอบสมมติฐาน 1 ประชากร")